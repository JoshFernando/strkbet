/// <reference types="bn.js" />
import { Numeric } from "./types";
import { BigNumberish } from "starknet/utils/number";
import { ec } from "elliptic";
import { HardhatRuntimeEnvironment } from "hardhat/types";
export declare function generateRandomStarkPrivateKey(length?: number): import("bn.js");
/**
 * Returns a signature which is the result of signing a message
 *
 * @param keyPair
 * @param accountAddress
 * @param nonce
 * @param functionSelector
 * @param toAddress
 * @param calldata
 * @returns the signature
 */
export declare function sign(keyPair: ec.KeyPair, accountAddress: string, nonce: BigNumberish, functionSelector: string, toAddress: string, calldata: BigNumberish[]): Numeric[];
export declare function handleAccountContractArtifacts(accountType: string, artifactsName: string, hre: HardhatRuntimeEnvironment): Promise<string>;
