import { Image, ProcessResult } from "@nomiclabs/hardhat-docker";
import { Choice } from "./types";
interface CompileWrapperOptions {
    file: string;
    output: string;
    abi: string;
    cairoPath: string;
}
interface DeployWrapperOptions {
    contract: string;
    gatewayUrl: string;
    inputs?: string[];
    salt?: string;
}
interface InvokeOrCallWrapperOptions {
    choice: Choice;
    address: string;
    abi: string;
    functionName: string;
    inputs?: string[];
    signature?: string[];
    wallet?: string;
    account?: string;
    accountDir?: string;
    networkID?: string;
    gatewayUrl: string;
    feederGatewayUrl: string;
    blockNumber?: string;
}
interface GetTxStatusWrapperOptions {
    hash: string;
    gatewayUrl: string;
    feederGatewayUrl: string;
}
interface DeployAccountWrapperOptions {
    wallet: string;
    accountName: string;
    accountDir: string;
    gatewayUrl: string;
    feederGatewayUrl: string;
    network: string;
}
export declare abstract class StarknetWrapper {
    protected prepareCompileOptions(options: CompileWrapperOptions): string[];
    abstract compile(options: CompileWrapperOptions): Promise<ProcessResult>;
    protected prepareDeployOptions(options: DeployWrapperOptions): string[];
    abstract deploy(options: DeployWrapperOptions): Promise<ProcessResult>;
    protected prepareInvokeOrCallOptions(options: InvokeOrCallWrapperOptions): string[];
    abstract invokeOrCall(options: InvokeOrCallWrapperOptions): Promise<ProcessResult>;
    protected prepareGetTxStatusOptions(options: GetTxStatusWrapperOptions): string[];
    abstract getTxStatus(options: GetTxStatusWrapperOptions): Promise<ProcessResult>;
    protected getPythonDeployAccountScript(options: DeployAccountWrapperOptions): string;
    abstract deployAccount(options: DeployAccountWrapperOptions): Promise<ProcessResult>;
}
export declare class DockerWrapper extends StarknetWrapper {
    private docker;
    private image;
    constructor(image: Image);
    private getDocker;
    compile(options: CompileWrapperOptions): Promise<ProcessResult>;
    deploy(options: DeployWrapperOptions): Promise<ProcessResult>;
    invokeOrCall(options: InvokeOrCallWrapperOptions): Promise<ProcessResult>;
    getTxStatus(options: GetTxStatusWrapperOptions): Promise<ProcessResult>;
    deployAccount(options: DeployAccountWrapperOptions): Promise<ProcessResult>;
}
export declare class VenvWrapper extends StarknetWrapper {
    private starknetCompilePath;
    private starknetPath;
    constructor(venvPath: string);
    private execute;
    compile(options: CompileWrapperOptions): Promise<ProcessResult>;
    deploy(options: DeployWrapperOptions): Promise<ProcessResult>;
    invokeOrCall(options: InvokeOrCallWrapperOptions): Promise<ProcessResult>;
    getTxStatus(options: GetTxStatusWrapperOptions): Promise<ProcessResult>;
    deployAccount(options: DeployAccountWrapperOptions): Promise<ProcessResult>;
}
export {};
